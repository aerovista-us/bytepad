# Disable symlinks to avoid Windows permission issues
# Use file copying instead
legacy-peer-deps=true
fund=false
audit=false
# Force npm to copy instead of symlink for local packages
package-lock=false

